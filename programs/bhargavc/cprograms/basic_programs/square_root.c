#include <math.h>
#include <stdio.h>

void main()
{
   int a;
   float squareRoot;
   printf("Enter a number: ");
   scanf("%d",&a);
   squareRoot = sqrt(a);
   printf("Square root of: %f", squareRoot);

}
